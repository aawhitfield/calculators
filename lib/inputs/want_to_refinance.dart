import 'package:flutter/material.dart';

class WantToRefinance extends StatelessWidget {
  const WantToRefinance({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
