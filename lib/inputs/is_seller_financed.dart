import 'package:flutter/material.dart';

class IsSellerFinanced extends StatelessWidget {
  const IsSellerFinanced({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
