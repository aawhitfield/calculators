import 'package:flutter/material.dart';

class HoldingCosts extends StatelessWidget {
  const HoldingCosts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
